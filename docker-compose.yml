services:
  airgap-package-builder:
    build: .
    container_name: airgap-package-builder
    ports:
      - "8000:8000"
    environment:
      AIRGAP_HOST_WORKDIR: ${PWD}/workdir
      AIRGAP_OUTPUT_UID: ${AIRGAP_OUTPUT_UID:-1000}
      AIRGAP_OUTPUT_GID: ${AIRGAP_OUTPUT_GID:-1000}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${PWD}/bundles:/app/bundles
      - ${PWD}/workdir:/app/workdir
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import json, urllib.request; json.load(urllib.request.urlopen('http://127.0.0.1:8000/api/health', timeout=3))",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    restart: unless-stopped
